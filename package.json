{
  "scripts": {
    "up": "git pull & git add . & git commit -m '.' & git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "vue-iscroll-view": "^1.0.3"
  }
}
