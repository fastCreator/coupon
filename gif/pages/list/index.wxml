<scroll-view scroll-y bindscrolltolower="nextPage">
  <view class='filter'>
    <view wx:for="{{navs}}" class="{{sort===index?'active':''}}" bindtap='toggleSort' data-index='{{index}}'>{{item}}</view>
  </view>
  <list list="{{list}}"></list>
</scroll-view>