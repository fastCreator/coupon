<view class="select">
  <image src="{{select}}" mode="aspectFit"></image>
</view>
<view class="title">{{title}}</view>
<view class="list">
  <image wx:for="{{list}}" src="{{item}}" bindtap='selectImg' data-select="{{item}}"></image>
</view>